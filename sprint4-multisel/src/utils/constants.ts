export const fontsPath = 'assets/fonts';

const texturesPath = 'assets/images/textures/';
const imgExt = '.jpg';

export const textures = {
    GRASS: texturesPath + 'grass' + imgExt,
    PAPER:  texturesPath + 'paper' + imgExt,
    STONE:  texturesPath + 'stone' + imgExt,
    WALLBRICK:  texturesPath + 'wall-brick' + imgExt,
    WOODEN:  texturesPath + 'wooden' + imgExt
}

export const clonePodiumId = 'clone-podium'